services:
  searxng:
    container_name: searxng
    image: searxng/searxng:latest@sha256:f433294b46a93564993c4371005341e013d94aa8ea4662d8ee521cd2cccb08e8
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - reverse-proxy-network
    volumes:
      - ./searxng.yaml:/etc/searxng/settings.yml:ro
      - searxng-cache:/var/cache/searxng
    environment:
      SEARXNG_BASE_URL: https://search.oscarcorner.com/
      SEARXNG_VALKEY_URL: valkey://valkey:6379/0
    labels:
      traefik.enable: true
      traefik.http.routers.searxng.entrypoints: websecure
      traefik.http.routers.searxng.rule: Host(`search.oscarcorner.com`)
      traefik.http.services.searxng.loadbalancer.server.port: 8080
      traefik.http.routers.searxng.tls.certresolver: route53

  valkey:
    container_name: searxng-valkey
    image: docker.io/valkey/valkey:9-alpine@sha256:a35428eba9043cc0b79dbe54100f0c92784f2de00ad09b01182bfb1c5c83d1bd
    command: valkey-server --save 30 1 --loglevel warning
    restart: unless-stopped
    volumes:
      - valkey-data:/data/
    networks:
      - reverse-proxy-network

networks:
  reverse-proxy-network:
    external: true

volumes:
  valkey-data:
  searxng-cache:
