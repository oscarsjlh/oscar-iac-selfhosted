services:
  seerr:
    image: ghcr.io/seerr-team/seerr:latest@sha256:f4768de5f616248d723e05891f3345a1402123775d03bf0890dbfedc0831bda1
    init: true
    networks:
      - reverse-proxy-network
    container_name: seerr
    environment:
      - LOG_LEVEL=debug
      - TZ=Europe/London
      - PORT=5055 #optional
    labels:
      traefik.enable: true
      traefik.http.routers.seer.entrypoints: websecure
      traefik.http.routers.seer.rule: Host(`requests.oscarcorner.com`)
      traefik.http.services.seer.loadbalancer.server.port: 5055
      traefik.http.routers.seer.tls.certresolver: route53
    volumes:
      - ./config:/app/config
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:5055/api/v1/settings/public || exit 1
      start_period: 20s
      timeout: 3s
      interval: 15s
      retries: 3
    restart: unless-stopped

networks:
  reverse-proxy-network:
    external: true
